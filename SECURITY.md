# Security Policy

Do not open issues or pull requests containing real server IPs, passwords, UUIDs, private keys, public subscription URLs, or generated client links.

If you accidentally commit secrets, rotate the affected protocol password/key immediately and purge the secret from Git history before making the repository public.
